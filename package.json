{
  "name": "pairingsengine",
  "version": "0.1.0",
  "private": true,
  "description": "Web-based chess tournament manager: FIDE Swiss pairings (JaVaFo), TRF export, printing, teams.",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "start": "npm run start -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
